# 📡 Conexión a MongoDB (ajusta según el servicio real en Railway)
MONGO_URI=mongodb://usuario:contraseña@mongodb.up.railway.app:27017
MONGO_DB=chatbot_db

# 🤖 Conexión a Rasa en Railway (modo público)
RASA_URL=https://rasa-production.up.railway.app/webhooks/rest/webhook

# 🔐 Seguridad
SECRET_KEY=tu_clave_secreta_segura
ACCESS_TOKEN_EXPIRE_MINUTES=60

# 📁 Ruta de logs
LOG_DIR=logs

# 🌐 Orígenes permitidos para CORS (puedes agregar Zajuna aquí)
ALLOWED_ORIGINS=https://midominio.com,https://zajuna.com,http://localhost:5173,http://localhost:3000

SECRET_KEY=2X@q4Jp9#8kfs93js...     # Genera con `python -c "import secrets; print(secrets.token_urlsafe(64))"`
DEBUG=true
ACCESS_TOKEN_EXPIRE_MINUTES=60