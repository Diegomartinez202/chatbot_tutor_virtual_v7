rest:
  url: "http://localhost:8000/api/chat"  # FastAPI como proxy para el widget

socketio:
  user_message_evt: user_uttered
  bot_message_evt: bot_uttered
  session_persistence: true

rasa:
  url: "http://localhost:5055/webhook"  # Action Server de Rasa para ejecutar actions.py
