rest:
  url: "http://backend:8000/api/chat"  # FastAPI como proxy

socketio:
  user_message_evt: user_uttered
  bot_message_evt: bot_uttered
  session_persistence: true

rasa:
  url: "http://action_server:5055/webhook"  # Action Server como servicio
